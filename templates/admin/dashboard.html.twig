{% extends '@EasyAdmin/layout.html.twig' %}

{% block content %}
    <div class="container">
        <h1 class="mb-4">Tableau de bord DogWalk</h1>
        <div class="row">
            {% for label, count in counts %}
                <div class="col-lg-3 col-md-4 col-sm-6 mb-4">
                    <div class="card p-3 text-center">
                        <div class="card-body">
                            <h5 class="card-title">{{ label|replace({'groupRequests':'Demandes de groupe','groupRoles':'Rôles de groupe','blockLists':'Liste de blocage'})|capitalize }}</h5>
                            <p class="display-4 mb-0">{{ count }}</p>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
